import React from 'react';

const ProductDetails = () => {
    return (
        <div>
            <h1>This is private route. and show the product details</h1>
        </div>
    );
};

export default ProductDetails;